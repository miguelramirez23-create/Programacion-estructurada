#include<studio.h>
#include<stdlib.h>
int main()
{
  float altura, base, largo; /*entradas de tipo flotante altura y base */
printf("altura de la rampa: ");/*altura de la rampa*/
scanf("%f", &alrura);
printf("base de la rampa: ");/*base de la rampa*/
scanf("%F, &base);
  largo=sqrt((altura*altura)+(base*base));/*Se resuelve con el metodo pitagoras*/
  printf("El largo de la rampa es: %3f\n", largo);/*resultado de la operacion */
return 0
  }
